env: "local" # local | dev | prod
storage_path: "./storage/storage.db"
http_server:
  address: "localhost:8080"
  timeout: 4s
  idle_timeout: 60s
  user: "admin"
  password: "admin" # выпилить при редактировании internal/config/config.go
